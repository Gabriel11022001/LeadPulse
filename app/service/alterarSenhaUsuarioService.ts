// alterar a senha do usuário na base de dados
const alterarSenhaUsuarioService = async (id: string, novaSenha: string) => {

  try {

  } catch (e) {
    console.log("Erro: " + e);

    throw e;
  }

}

export default alterarSenhaUsuarioService;