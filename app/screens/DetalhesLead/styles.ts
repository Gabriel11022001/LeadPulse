import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  container: {
    width: "90%",
    marginStart: "5%",
    marginEnd: "5%",
    marginTop: 20,
    marginBottom: 20,
    borderRadius: 12,
    backgroundColor: "#fff",
    padding: 15
  }

});

export default styles;