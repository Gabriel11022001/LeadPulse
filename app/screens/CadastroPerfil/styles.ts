import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  titulo: {
    color: "#000",
    fontWeight: 900,
    fontSize: 30,
    textAlign: "center",
    marginBottom: 10
  },
  subtitulo: {
    color: "#000",
    fontSize: 16,
    textAlign: "center",
    marginBottom: 30
  }

});

export default styles;